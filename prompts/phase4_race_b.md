You are one of THREE agents racing in isolated git worktrees on the FINAL blocker of the Mischief Makers audio microcode. First verified fix wins. Work fast, verify honestly.

STATE OF KNOWLEDGE (read PHASE2_NOTES.md + PHASE3_NOTES.md + src/rsp/rsp_callbacks.cpp first — then this): The game boots, RT64 renders 3 frames of display lists, then stalls because the audio RSP task never completes. The recompiled aspMain (src/rsp/generated/aspMain.cpp, regenerate with `tools/N64Recomp/build/RSPRecomp aspMain.us1.rsp.toml`) is a SDK-2.0-era build (text at ROM 0xBCAB0, 0xE20 bytes; data blob at ROM 0xEFE10, 0x2C0 bytes; IMEM base 0x1000). Facts established by the driver:
- The audio OSTask: data_ptr=0x80137DD0 (command list, size 0x890), ucode_data=0x800EF210 (0x2C0). librecomp's run_task loads ucode_data to DMEM[0] and the OSTask to DMEM[0xFC0]; NOTHING loads the command list — the driver added a first-chunk prefetch (min(size,0x140) bytes -> DMEM 0x380) in rsp_callbacks.cpp, which advanced the ucode from dispatching zeros to processing a real command.
- Command loop at IMEM 0x1064: reads 8-byte commands at $29 (0x380), opcode=(word0>>23)&0xFE... wait, >>23 then &0xFE = table BYTE offset; dispatch table = halfwords at DMEM 0x10 (from the ucode data blob); `jr $2` (NOT jalr — $ra untouched, stays 0x1040 from the entry jal).
- Current infinite loop signature: dispatch alternates 0x1040 <-> 0x12D0 forever (sampled via gdb breakpoint on the dispatch switch, aspMain.cpp:2239, printing jump_target). 0x12D0's handler appears to end with `j 0x1118` (= the tail of the DMA-READ helper: mtc0 $1 SP_MEM_ADDR; mtc0 $2 SP_DRAM_ADDR; mtc0 $3 SP_RD_LEN; jr $31). But the dispatch clobbers $1 (op*2) and $2 (handler addr) — so how this works on real hardware is THE open question. The driver's j-target decoding may contain errors (mask assumptions) — re-derive from raw bytes, do not trust prior disassembly.
- SM64's decomp has LABELED source of the same-family ucode: https://raw.githubusercontent.com/n64decomp/sm64/master/rsp/audio.s (fetch it; handler register contract per it: $1=DMEM addr, $2=DRAM addr, $3=len-1, set by handlers from command params).
- RSPRecomp hardcodes mfc0 DPC_STATUS/SP_DMA_BUSY/SP_SEMAPHORE reads to 0 and models DMA via dma_rdram_to_dmem/dma_dmem_to_rdram (lib/N64ModernRuntime/librecomp/include/librecomp/rsp.hpp — READ IT; note dram_addr &= 0xFFFFF8 masking).

RULES: own worktree only. Symlinked dirs (RecompiledFuncs, lib/N64ModernRuntime, tools/N64Recomp, lib/rt64, input, reference) are SHARED READ-ONLY; to modify the runtime, privatize first: `rm lib/N64ModernRuntime && cp -rL /home/thiago/repos/trouble-makers-pc-recomp/lib/N64ModernRuntime lib/`. You MAY temp-edit your regenerated src/rsp/generated/aspMain.cpp for tracing (it's regenerated output — final fix must NOT hand-edit it; fixes go in the toml, rsp_callbacks.cpp, or a documented runtime patch). Commit nothing.
BUILD: `cmake -B build -DMM_BUILD_GRAPHICS=OFF && cmake --build build --target mm_game -j8` (first regenerate aspMain). RUN: `cp ~/repos/trouble-makers-ai-recomp/baserom.us1.z64 ./rom.z64` once, then `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy timeout -k 5 30 ./build/src/game/mm_game ./rom.z64`. Existing [gfx]/[mm_rsp] stderr diagnostics show send_dl count. GDB: attach blocked; launch under gdb (`timeout --foreground -s INT 20 gdb -batch -x script.gdb --args ...`). Never pkill -f a pattern matching your own command line.

SUCCESS GATE — FIXED requires ALL of: (1) the mechanism of the 1040<->12D0 loop explained with byte-level evidence; (2) a clean fix (no hand-edits to generated code); (3) a 30s run where the audio task COMPLETES repeatedly (add a temp exit-reason trace: wrap aspMain in rsp_callbacks.cpp) AND send_dl exceeds 100 (game frame loop flowing); (4) PHASE4_NOTES_<lane>.md with root cause + evidence. End your final message with:
RESULT
gate: <FIXED|DIAGNOSED|FAILED>
root_cause: <one line>
evidence: <one line>

YOUR LANE (b — recompiler-semantics audit): Hypothesis: RSPRecomp mis-models something this old ucode relies on. Audit the generated aspMain.cpp against the raw instruction stream for: (1) branch/jump DELAY SLOT handling around the dispatch `jr $2` at 0x108C and around handler `j` instructions — a delay-slot instruction executed (or not executed) wrongly changes register state; (2) the hardcoded-0 reads (DPC_STATUS, SP_DMA_BUSY, SP_SEMAPHORE, SP_DMA_FULL) — find every read site in the raw bytes and reason what hardware value the ucode EXPECTS (e.g. semaphore acquire loops that must eventually see 0 vs 1); (3) DMA modeling: DO_DMA_READ/WRITE fire immediately on the RD_LEN/WR_LEN write — on hardware the mtc0 MEM/DRAM writes at 0x111C/0x1120 happen BEFORE the length write, but if a handler jumps into the MIDDLE of the helper (e.g. entering at 0x1118 vs 0x1104), which mtc0s actually execute? Compare RSPRecomp's translation of tools/N64Recomp/RSPRecomp/src/rsp_recomp.cpp for how it handles these. Fix at the right layer (toml options, runtime patch with privatized copy, or a documented RSPRecomp patch).
