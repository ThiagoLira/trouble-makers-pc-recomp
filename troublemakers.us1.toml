# N64Recomp configuration for Mischief Makers (US 1.1)
# input elf: the byte-perfect decomp build from trouble-makers-ai-recomp
[input]
entrypoint = 0x80000400
elf_path = "input/troublemakers.elf"
output_func_path = "RecompiledFuncs"

# Static (file-local) functions whose symbols the decomp ELF drops — the
# recompiler sees a symbol-table hole and skips their code, but the game
# installs several of them as function POINTERS (libaudio sequencer
# handlers, rmon internals), so they must be translated. Names/addrs from
# the decomp's symbol_addrs; sizes from the decomp build's .o files
# (fallback: gap to the next known function).
manual_funcs = [
    { name = "__CSPVoiceHandler", section = ".main", vram = 0x8009C6AC, size = 0x82C },  # gap
    { name = "__CSPHandleNextSeqEvent", section = ".main", vram = 0x8009CF78, size = 0x11C },  # gap
    { name = "__CSPHandleMIDIMsg", section = ".main", vram = 0x8009D094, size = 0xDAC },  # gap
    { name = "__CSPHandleMetaMsg", section = ".main", vram = 0x8009DE40, size = 0x2C8 },  # gap
    { name = "__CSPRepostEvent", section = ".main", vram = 0x8009E108, size = 0x128 },  # gap
    { name = "__setUsptFromTempo", section = ".main", vram = 0x8009E230, size = 0x70 },  # gap
    { name = "_timeToSamplesNoRound", section = ".main", vram = 0x8009ECD8, size = 0x68 },  # gap
    { name = "__nextSampleTime", section = ".main", vram = 0x8009ED84, size = 0xEC },  # gap
    { name = "__alCSeqGetTrackEvent", section = ".main", vram = 0x8009F288, size = 0x44C },  # gap
    { name = "__getTrackByte", section = ".main", vram = 0x8009FB14, size = 0x1F4 },  # gap
    { name = "__readVarLen", section = ".main", vram = 0x8009FD08, size = 0x98 },  # gap
    { name = "drawbitmap", section = ".main", vram = 0x800A00BC, size = 0x28A0 },  # gap
    { name = "proutSprintf", section = ".main", vram = 0x800A56BC, size = 0x54 },  # gap
    { name = "proutSyncPrintf", section = ".main", vram = 0x800A57F0, size = 0x94 },  # gap
    { name = "rmonPutchar", section = ".main", vram = 0x800A5884, size = 0x80 },  # gap
    { name = "kmc_proutSyncPrintf", section = ".main", vram = 0x800A5904, size = 0x230 },  # gap
    { name = "SetTempBreakpoint", section = ".main", vram = 0x800A9B00, size = 0xC8 },  # gap
    { name = "ClearTempBreakpoint", section = ".main", vram = 0x800A9BC8, size = 0x124 },  # gap
    { name = "IsJump", section = ".main", vram = 0x800AA304, size = 0xB0 },  # gap
    { name = "rmonSendBreakMessage", section = ".main", vram = 0x800AA4D8, size = 0xC8 },  # gap
    { name = "rmonFindFaultedThreads", section = ".main", vram = 0x800AA684, size = 0xFC },  # gap
    { name = "__seqpVoiceHandler", section = ".main", vram = 0x800AB4B0, size = 0x774 },  # gap
    { name = "__handleNextSeqEvent", section = ".main", vram = 0x800ABD2C, size = 0x108 },  # gap
    { name = "__handleMIDIMsg", section = ".main", vram = 0x800ABE34, size = 0xC98 },  # gap
    { name = "__handleMetaMsg", section = ".main", vram = 0x800ACACC, size = 0xA0 },  # gap
    { name = "_Putfld", section = ".main", vram = 0x800B3BA4, size = 0x9AC },  # gap
    { name = "__osDefaultHandler", section = ".main", vram = 0x800B4550, size = 0x60 },  # gap
    { name = "string_to_u32", section = ".main", vram = 0x800B45B0, size = 0x70 },  # gap
    { name = "send_packet", section = ".main", vram = 0x800B4620, size = 0x7C },  # gap
    { name = "clear_IP6", section = ".main", vram = 0x800B469C, size = 0x78 },  # gap
    { name = "send", section = ".main", vram = 0x800B4714, size = 0xE8 },  # gap
    { name = "__osLeoAbnormalResume", section = ".main", vram = 0x800B51FC, size = 0xF8 },  # gap
    { name = "__osLeoResume", section = ".main", vram = 0x800B52F4, size = 0x11C },  # gap
    { name = "NotImplemented", section = ".main", vram = 0x800B5840, size = 0x1C },  # gap
    { name = "LoadStoreSU", section = ".main", vram = 0x800B5990, size = 0x8C },  # gap
    { name = "LoadStoreVU", section = ".main", vram = 0x800B5A1C, size = 0x9C },  # gap
    { name = "SetUpForRCPop", section = ".main", vram = 0x800B5AB8, size = 0xB0 },  # gap
    { name = "CleanupFromRCPop", section = ".main", vram = 0x800B5B68, size = 0xB0 },  # gap
    { name = "rmonGetRcpRegister", section = ".main", vram = 0x800B6068, size = 0x74 },  # gap
    { name = "read8", section = ".main", vram = 0x800B7A88, size = 0x30 },  # gap
    { name = "read16", section = ".main", vram = 0x800B7AB8, size = 0x58 },  # gap
    { name = "read32", section = ".main", vram = 0x800B7B10, size = 0xA0 },  # gap
    { name = "readVarLen", section = ".main", vram = 0x800B7BB0, size = 0xA0 },  # gap
    { name = "_Ldunscale", section = ".main", vram = 0x800B9904, size = 0x104 },  # gap
    { name = "_Genld", section = ".main", vram = 0x800B9A08, size = 0x788 },  # gap
]

# Widescreen: widen the per-frame view-cull rectangle at translation time.
# The game culls actor/sprite DRAWING against a rect it recomputes every
# frame as camX +/- 0x90 (144px) horizontally (Camera_UpdateViewBounds at
# 0x800462F0, plus a LEFT re-set in func_800463C0). Patching the addiu
# immediates to +/- 0x120 (288px, enough for 21:9) makes wing content update
# deterministically — the previous host-thread mirror raced the game's
# per-frame rewrite and sprites flickered in the wings. In 4:3 the extra
# margin only draws objects RT64 clips; verified no gameplay impact.
# Y bounds (+/- 0x70) are left untouched.
[patches]
instruction = [
    { func = "Camera_UpdateViewBounds", vram = 0x80046370, value = 0x2443FEE0 }, # LEFT  = camX - 0x120
    { func = "Camera_UpdateViewBounds", vram = 0x80046398, value = 0x24440120 }, # RIGHT = camX + 0x120
    { func = "func_800463C0",           vram = 0x800463E0, value = 0x25CFFEE0 }, # LEFT re-set = camX - 0x120

    # REAL-CONTENT WIDESCREEN — scrolling background band (func_80082820).
    # The band is a 7-row x 10-col grid of 32px CI8 tiles (exactly 320px) read
    # from the dst array D_80180D90 (140 s32 slots, filled 70 at stride 10).
    # Its ultimate source is the level's backdrop tilemap: a 16x16 byte grid
    # that WRAPS on both axes (col = (srcX>>5)&0xF, rowblk = (srcY>>1)&0xF0),
    # so tiles 5 columns beyond each 4:3 edge are genuine neighboring map
    # tiles, not mirrored/stretched pixels. These 5 instruction patches widen
    # the draw loop to 20 columns starting 5 left (cols -5..14) with an 80-byte
    # (20-slot) row stride and the two 4:3 left-edge assumptions removed; the
    # entry hook below rebuilds D_80180D90 at stride 20 with all 20 real
    # columns. Fill path + every word here ROM-verified (Phase 8 forensics).
    { func = "func_80082820", vram = 0x800829D0, value = 0x2414000F }, # col limit s4 10 -> 15
    { func = "func_80082820", vram = 0x80082A08, value = 0x240CFFFB }, # col start t4 0 -> -5
    { func = "func_80082820", vram = 0x80082A20, value = 0x24F900C0 }, # left-edge skip 0x20 -> 0xC0
    { func = "func_80082820", vram = 0x80082B24, value = 0x04010003 }, # drop max(0,upper_x) clamp
    { func = "func_80082820", vram = 0x80082A00, value = 0x00197900 }, # dst row stride <<3 -> <<4 (80B)

    # REAL-CONTENT WIDESCREEN — shared static tile-grid draw func_80082380
    # (midground D_80180930 / env D_80180B60 / static-bg D_80180D90 / possible
    # overlay callers). Loop is hardcoded 7 rows x 10 cols reading a stride-10
    # 70-slot array, so EVERY caller passes that layout. Same 10->20 widening
    # as func_80082820; the entry hook below repacks WHATEVER buffer arrives
    # into scratch at stride 20 (catch-all — the first attempt guarded only the
    # 3 known buffers, and an unlisted overlay caller's stride-10 data was
    # misread as stride-20 = stale-texture garbage in the wings). Known layers
    # get real map content in the wings; unknown callers get black wings and a
    # byte-identical center. All 5 words ROM-verified.
    { func = "func_80082380", vram = 0x8008252C, value = 0x2413000F }, # col limit s3 10 -> 15
    { func = "func_80082380", vram = 0x80082534, value = 0x240DFFFB }, # col start t5 0 -> -5
    { func = "func_80082380", vram = 0x8008254C, value = 0x24EE00C0 }, # left-edge skip 0x20 -> 0xC0
    { func = "func_80082380", vram = 0x80082644, value = 0x04010003 }, # drop max(0,upper_x) clamp
    { func = "func_80082380", vram = 0x800826F4, value = 0x27190050 }, # dst row stride 0x28 -> 0x50 (20 slots)

    # ENTITY POP-IN — the view-cull rect above is only one of the game's
    # visibility systems. Three more windows still used 4:3-sized X margins,
    # so objects appeared/vanished visibly inside the 16:9 wings (needs
    # ~+/-214px). Y margins untouched everywhere (vertical view unchanged).
    # All words verified against the community decomp asm.
    #
    # func_80016D94: per-frame despawner. Kills ACTOR_FLAG_ONSCREEN_ONLY
    # actors (projectiles, particles, drops) outside camX +/- 0xD0 — INSIDE
    # the wings. Widen to +/-0x150, keeping 48px slack over the 0x120 cull.
    # Pure despawn (slot reclamation); widening only delays cleanup.
    { func = "func_80016D94", vram = 0x80016E20, value = 0x28C1FEB0 }, # slti kill-left  -0xD0 -> -0x150
    { func = "func_80016D94", vram = 0x80016E28, value = 0x28C10151 }, # slti kill-right  0xD1 ->  0x151
    # func_800451E4: gem/prop sprite windowing. Rebuilt statelessly from the
    # world-item table every frame; an item only gets one of the 64 sprite
    # slots inside camX +/- 0xA8 (168px) — pops ~46px inside the wing. The
    # alternate wide-mode window (D_800BE710, +/-0x100) already covers 16:9.
    { func = "func_800451E4", vram = 0x8004527C, value = 0x2457FF18 }, # addiu window-left  -0xA8 -> -0xE8
    { func = "func_800451E4", vram = 0x80045280, value = 0x245E00E8 }, # addiu window-right  0xA8 ->  0xE8
    # Actor_IsOutsideRegion(actor, len): shared |x| > 0x90+len test used by
    # 19 sites (positional SFX radius + a few behavior gates). 0x90 is the
    # 4:3 half-width+margin; raise to 0xD8 (216 ~ the 16:9 half-width).
    { func = "Actor_IsOutsideRegion", vram = 0x80028924, value = 0x270900D8 }, # +0x90 -> +0xD8
    { func = "Actor_IsOutsideRegion", vram = 0x80028938, value = 0x240AFF28 }, # -0x90 -> -0xD8
]

# Layer-hook architecture: the two draw funcs get entry hooks that call into
# src/game/widescreen.cpp (mm_ws_band_repack / mm_ws_static_repack). Each
# repack copies the game's 7x10 center columns verbatim into a stride-20
# scratch grid and computes the 5 wing columns per side from the SAME
# wrapping-map formulas the game's fill func_8001107C uses (all parallax
# modes), validated per-tile against the loaded texture bank. The hooks also
# remap the display-list arena argument ($a0) to enlarged scratch arenas —
# the originals hold exactly 70 tiles and the widened loops can emit 140
# (see PHASE7_NOTES_widescreen.md, "display-list arena overflow").
hook = [
    # The game draws 14px/18px black strips at x=0 and x=302 around its
    # original 4:3 safe area. They become seams in an expanded framebuffer.
    # Suppress only those vertical rectangles in widescreen; top/bottom bars
    # and dynamically animated letterboxing remain untouched.
    { func = "Gfx_DrawBorderRect", text = '{ static int wide = -1; if (wide < 0) { extern char* getenv(const char*); char* e = getenv("MM_WIDESCREEN_ACTIVE"); wide = (e && e[0] == 0x31); } extern int mm_widescreen_gameplay_active(uint8_t*); int gamewide = mm_widescreen_gameplay_active(rdram); if (wide && gamewide && (((int32_t)ctx->r5 == 0 && (int32_t)ctx->r7 <= 14) || ((int32_t)ctx->r5 >= 302 && (int32_t)ctx->r7 == 320))) { return; } }' },
    # func_80026428 reloads the map bank for scenes 10, 24 and 46..51 but
    # never updates the recorded decompressed extent at 0x80137724/28 that
    # the wing residency check relies on. Capture Trouble_RLE_Type1's return
    # value ($v0 = decompressed size) right after the call returns.
    { func = "func_80026428", before_vram = 0x80026484, text = '{ extern void mm_ws_fix_bank_bound(uint8_t*, recomp_context*); mm_ws_fix_bank_bound(rdram, ctx); }' },
    { func = "func_80026220", text = '{ extern void mm_ws_bank_reloaded(uint8_t*, recomp_context*); mm_ws_bank_reloaded(rdram, ctx); }' },
    # DEBUG WARP (MM_WARP=<scene id> or exact MM_WARP_STAGE=<row>): the state
    # dispatcher runs every
    # frame; on the first frame the game reaches the title screen (gGameState
    # 0x800BE4F0 == 2), resolve the scene's complete progression-table row and
    # drop into LOADING (5, gGameStateSubState 0) — matching the stage-select
    # menu instead of reusing stale map/overlay assets. Skips the intro/attract
    # wait for widescreen iteration. Optional MM_WARP_AT overrides the trigger
    # state (default 2 = title; 1 = intro to skip more).
    { func = "func_80001670", text = '{ extern void mm_widescreen_sync_mode(uint8_t*); mm_widescreen_sync_mode(rdram); static int fc = 0; static int warped = 0; fc++; { extern char* getenv(const char*); char* te = getenv("MM_TEST_AUTO_ADVANCE"); if (te && te[0] == 0x31 && (fc % 120) == 0 && MEM_HU(0, (gpr)(int32_t)0x800BE4F0) == 6) { int buttons = MEM_HU(0, (gpr)(int32_t)0x800BE52C) | MEM_HU(0, (gpr)(int32_t)0x800BE530); MEM_H(0, (gpr)(int32_t)0x800BE4FC) = MEM_HU(0, (gpr)(int32_t)0x800BE4FC) | buttons; MEM_H(0, (gpr)(int32_t)0x800BE4F8) = MEM_HU(0, (gpr)(int32_t)0x800BE4F8) | buttons; } } if (!warped) { extern char* getenv(const char*); extern int atoi(const char*); char* e = getenv("MM_WARP"); char* se = getenv("MM_WARP_STAGE"); if (e != 0 || se != 0) { char* a = getenv("MM_WARP_AT"); int at = a ? atoi(a) : 1; char* d = getenv("MM_WARP_DELAY"); int dl = d ? atoi(d) : 90; int gs = MEM_HU(0, (gpr)(int32_t)0x800BE4F0); if (gs == at && fc > dl) { int requested_scene = e ? atoi(e) : -1; int requested_stage = se ? atoi(se) : -1; extern int mm_debug_warp_prepare(uint8_t*, int, int); int warp_scene = mm_debug_warp_prepare(rdram, requested_scene, requested_stage); MEM_H(0, (gpr)(int32_t)0x800BE5D0) = (int16_t)warp_scene; MEM_H(0, (gpr)(int32_t)0x800BE4EC) = 0; MEM_H(0, (gpr)(int32_t)0x800D2908) = 1; MEM_H(0, (gpr)(int32_t)0x800BE4F0) = 5; MEM_H(0, (gpr)(int32_t)0x800BE4F4) = 0; { int i; for (i = 0; i < 0x1000; i++) { MEM_W(i*4, (gpr)(int32_t)0x8027CEE8) = 0; } for (i = 0; i < 0xC00; i++) { MEM_W(i*4, (gpr)(int32_t)0x8027EEE8) = 0; } } warped = 1; } } } }' },
    # Layer repacks: rebuild each 7x10 layer grid at stride 20 with real
    # map-derived wing columns (exact func_8001107C formulas, all parallax
    # modes) and a per-tile texture-residency check in place of the former
    # per-scene blacklists. Logic lives in src/game/widescreen.cpp.
    { func = "func_80082820", text = '{ extern void mm_ws_band_repack(uint8_t*, recomp_context*); mm_ws_band_repack(rdram, ctx); }' },
    { func = "func_80082380", text = '{ extern void mm_ws_static_repack(uint8_t*, recomp_context*); mm_ws_static_repack(rdram, ctx); }' },
]
