You are one of TWO agents working INDEPENDENT lanes toward real widescreen support for the Mischief Makers recomp. The game is fully playable (see README.md + point_your_ai_agent_here/README.md — READ THE LATTER FIRST, it has the debugging recipes, repro harness, and repo rules that will save you hours).

CONTEXT: with `--widescreen` (RT64 AspectRatio::Expand) the render field widens, but the game culls/paints only its 4:3 region: side "wings" accumulate STALE framebuffer content (sprites/scenery that crossed the edge freeze there — evidence the game does NOT hard-scissor; it draws past x=320 but never clears there). Goal: fill the wings properly. The decomp (~/repos/trouble-makers-ai-recomp, READ-ONLY) is only ~12% matched to C — most game code is asm under asm/nonmatchings/; data symbols in versions/us1/symbol_addrs_data.txt. SCREEN_WIDTH define exists but is baked into compiled code — the lever must be RUNTIME variables/patches.

RULES: own worktree only; symlinked dirs read-only; privatize lib/N64ModernRuntime or lib/rt64 (cp -rL from /home/thiago/repos/trouble-makers-pc-recomp/lib/...) before modifying; commit nothing. Headless harness runs the full game loop but you CANNOT see pixels — design for the driver to verify visually: deliver switchable experiments (env vars / clearly marked constants) + exact instructions for what the driver should look for on screen. gdb: launch-only (no attach); rdram aligned-word reads are host-order.

DELIVERABLE: PHASE6_NOTES_<lane>.md + RESULT block (gate: PASSED|PARTIAL|FAILED, summary two lines).

YOUR LANE (a — find the width levers in the game): Identify the runtime variables/code that bound horizontal drawing. Method suggestions (pick what works): (1) data-symbol spelunking — symbol_addrs_data.txt names camera/view/screen variables (grep Camera, View, Screen, Scroll, Clip, Cull); cross-reference which asm functions read them (asm/nonmatchings, grep the %hi/%lo pairs); (2) the layer systems behind gDrawBackground/gDrawMidground/gDrawEnvLayer — find their draw loops and what limits their horizontal span (tile count loops? pixel bounds?); (3) empirical: in your worktree's src/game/ add an env-gated experiment hook (e.g. a 1ms thread or on_init poke like the AI_LEN mirror pattern in register_overlays.cpp) that writes candidate rdram variables (camera half-width, cull margins) to widened values; run headless to confirm no crash. Deliver: the variable map (address, meaning, evidence), a MM_WIDE=1 experiment patch that widens the most promising lever(s), and precise driver verification instructions ("run --widescreen, look at whether trees now draw live in the wings during the intro pan").
