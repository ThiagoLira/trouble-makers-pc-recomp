CONTEXT (all lanes): You are one of THREE agents racing in parallel on the same Phase 3 blocker, each in an isolated git worktree with a different attack lane. First verified fix wins; work fast but honestly.

THE BUG: mm_game boots deep — ROM validates, runtime + audio init, game thread runs Thread_MainProc, Sound_Update executes, one aspMain audio RSP task runs — then the game's threads PARK on a message queue with ~0 CPU forever. One earlier run instead SPUN HOT (100% × several cores) — the behavior is timing-dependent. Required reading before anything else: PHASE2_NOTES.md (the whole first-boot debugging chain, esp. item 6: Sound_Update reads AI_LEN_REG raw at 0xA4500004; a zero-fill page at rdram+0x24500000 makes it always read 0), PHASE2_NOTES_w2.md (aspMain jump tables), and the decomp's boot.c + music.c (~/repos/trouble-makers-ai-recomp — READ-ONLY).

YOUR WORKSPACE: work ONLY in your worktree (you are cd'd into it). RecompiledFuncs/, input/, reference/, src/rsp/generated/, lib/N64ModernRuntime, lib/rt64, tools/N64Recomp are SYMLINKS into the main checkout SHARED with the other agents — treat them as strictly READ-ONLY. If your fix requires editing the runtime (lib/N64ModernRuntime), first privatize it: `rm lib/N64ModernRuntime && cp -rL /home/thiago/repos/trouble-makers-pc-recomp/lib/N64ModernRuntime lib/` — then edit your private copy freely. Commit NOTHING (driver reviews + commits the winner).

BUILD (headless, no RT64 — faster, stub renderer): `cmake -B build -DMM_BUILD_GRAPHICS=OFF && cmake --build build --target mm_game -j8`. Note the stub renderer reports valid()==false so the gfx thread exits at startup — account for that when interpreting behavior (M_GFXTASK routing differs from the real-renderer run).
RUN: `cp ~/repos/trouble-makers-ai-recomp/baserom.us1.z64 ./rom.z64` (once), then `SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy timeout -k 5 30 ./build/src/game/mm_game ./rom.z64`.
DEBUG: ptrace attach is blocked (yama) — launch UNDER gdb instead: `timeout --foreground -s INT 25 gdb -batch -ex run -ex "thread apply all bt 8" --args ./build/src/game/mm_game ./rom.z64`. To inspect game state: rdram is the flat game memory (N64 vaddr - 0x80000000); decomp symbol addresses are in ~/repos/trouble-makers-ai-recomp/versions/us1/symbol_addrs*.txt. Temp diagnostic prints anywhere in YOUR worktree are fine (remove them from the final patch).
GOTCHAS: never `pkill -f` a pattern that matches your own command line; mm_game sometimes ignores plain SIGTERM — always `timeout -k`.

SUCCESS GATE — FIXED means all of: (1) root cause identified with evidence; (2) baseline reproduced (park or spin within 30s); (3) with your MINIMAL clean fix applied, a 30s run demonstrably progresses past the park — acceptable markers: repeated M_AUDTASK dispatches over the whole 30s (add a counter log in your rsp_callbacks), an M_GFXTASK submission, or gdb showing an identifiably later boot function executing; (4) diagnostics stripped from the final diff. Write PHASE3_NOTES_<lane>.md with root cause, evidence, fix rationale. End your final message with:
RESULT
gate: <FIXED|DIAGNOSED|FAILED>
root_cause: <one line>
evidence: <one line>

YOUR LANE (b — message-queue forensics): Identify the EXACT queue the game parks on, no hypotheses accepted without the address. Method: gdb the parked state, walk up from the blocked [Game] thread frames (osRecvMesg path) to recover the PTR(OSMesgQueue) argument (an rdram offset — add 0x80000000 and match against the decomp's data symbols in symbol_addrs*.txt to NAME the queue); enumerate every osSetEventMesg/osViSetEvent/Sound_SetEventMesg registration the game makes during boot (decomp boot.c/music.c + breakpoints on the librecomp wrappers) and which events librecomp/ultramodern actually post. The fix is whatever missing signal that forensic diff exposes — implement it game-side (src/game/) if possible. Also explain the park-vs-spin nondeterminism if your queue map accounts for it.
