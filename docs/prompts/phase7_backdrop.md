You are extending widescreen support for the Mischief Makers recomp. READ FIRST: docs/README.md (rules, harness, gotchas), then PHASE6_NOTES_a.md (the cull-rect forensics — your starting map), then this.

STATE: --widescreen works: view-cull is widened at translation time (troublemakers.us1.toml [patches], camX±0x120), RT64 clears the wings (patches/rt64/0001), ground/sky/mountain layers and sprites render live in the wings. REMAINING GAP (your mission): the mid-screen BACKDROP BAND — the tree-line/meadow "wall" layers — still ends at the 4:3 width, leaving black bands in the wings between the extended sky and extended ground. PHASE6_NOTES_a.md flagged the suspected mechanism: background tile layers driven by a runtime tile count `D_8013769C` + tile list `D_80137614`, populated by `func_800255B4` ("riskier: content bounded").

YOUR MISSION: make the backdrop band extend into the wings — most plausible shape: more tiles drawn per row (repeating/clamping the edge tiles), or a per-layer span variable. Steps: (1) understand the layer draw path (decomp asm around func_800255B4 + the gDrawBackground/gDrawMidground/gDrawEnvLayer systems; data symbols near D_80137614); (2) determine whether tile GEOMETRY positions come from level data (fixed world positions — then the layer already exists wider in world space and only the CULL clips it... test that hypothesis FIRST: it may be another ±margin variable/immediates like the view-cull was, in the layer-draw or scroll code) or from a screen-relative span count; (3) implement the least-invasive widening: another [patches] instruction entry in the toml (follow the existing pattern; verify instruction addresses against the ROM — .main vram V maps to ROM offset V-0x80000400+0x1000; sanity-check a known word first) OR an env-gated runtime poke in src/game/ if it's a variable. NO racy per-ms mirrors of per-frame-rewritten values (that caused sprite flicker before — translation-time patches or one-shot variable pokes only).
VERIFICATION: headless gate = game runs 60s+ crash-free with your change (the attract demo plays level-1 gameplay automatically ~25s in — it exercises the real layer-draw paths). Visual verification is the driver's. Deliver PHASE7_NOTES.md: mechanism map with evidence, the change, what the driver should see. RESULT block: gate PASSED|PARTIAL|FAILED + two-line summary.
