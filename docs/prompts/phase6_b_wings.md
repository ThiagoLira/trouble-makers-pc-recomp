You are one of TWO agents working INDEPENDENT lanes toward real widescreen support for the Mischief Makers recomp. The game is fully playable (see README.md + docs/README.md — READ THE LATTER FIRST, it has the debugging recipes, repro harness, and repo rules that will save you hours).

CONTEXT: with `--widescreen` (RT64 AspectRatio::Expand) the render field widens, but the game culls/paints only its 4:3 region: side "wings" accumulate STALE framebuffer content (sprites/scenery that crossed the edge freeze there — evidence the game does NOT hard-scissor; it draws past x=320 but never clears there). Goal: fill the wings properly. The decomp (~/repos/trouble-makers-ai-recomp, READ-ONLY) is only ~12% matched to C — most game code is asm under asm/nonmatchings/; data symbols in versions/us1/symbol_addrs_data.txt. SCREEN_WIDTH define exists but is baked into compiled code — the lever must be RUNTIME variables/patches.

RULES: own worktree only; symlinked dirs read-only; privatize lib/N64ModernRuntime or lib/rt64 (cp -rL from /home/thiago/repos/trouble-makers-pc-recomp/lib/...) before modifying; commit nothing. Headless harness runs the full game loop but you CANNOT see pixels — design for the driver to verify visually: deliver switchable experiments (env vars / clearly marked constants) + exact instructions for what the driver should look for on screen. gdb: launch-only (no attach); rdram aligned-word reads are host-order.

DELIVERABLE: PHASE6_NOTES_<lane>.md + RESULT block (gate: PASSED|PARTIAL|FAILED, summary two lines).

YOUR LANE (b — clean wings now, at the renderer): Independently of lane a, make widescreen non-broken TODAY: the extended side regions must not accumulate stale content. Investigate in RT64 (privatize lib/rt64 if you must patch): how does AspectRatio::Expand composite the areas outside the game's drawn region — is there a per-frame clear of the extended framebuffer regions, an extended-GBI clear mechanism (grep gEX/extended in src/hle), or a place in the framebuffer manager where the extended margins could be cleared to the VI border color each frame? Alternative approach if RT64-internal is too invasive: in our mm_graphics glue (src/graphics/rt64_render_context.cpp), inject synthetic clear commands into the display list per frame (prepend a fillrect covering the extended regions — check how RT64's processDisplayLists could be fed a small host-built prologue DL; the decomp's gbi.h documents F3D fillrect encoding). Deliver: working env-gated implementation (MM_CLEAR_WINGS=1) + driver verification instructions. PARTIAL = precise mechanism map + why blocked.
