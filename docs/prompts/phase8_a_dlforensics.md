DEEP-DIVE CAMPAIGN, funded for thoroughness — the user has authorized maximum effort. You are one of THREE agents on the last visual gap in widescreen support. READ FIRST: point_your_ai_agent_here/README.md (rules, harness, debugging recipes — including WORKSPACE RULE 3.5), then point_your_ai_agent_here/drafts/backdrop_band_patches.toml.draft INCLUDING BOTH FAILURE POST-MORTEMS at the bottom, then PHASE6_NOTES_a.md.

THE PROBLEM: in --widescreen, a mid-screen backdrop band (parallax treeline/wall behind gameplay) still ends at the 4:3 edge, leaving black bands in the wings between the (working) extended sky and (working) extended ground. TWO fix attempts failed because they patched WITHOUT ground truth about which code+data actually draws that band. Your wave establishes the ground truth first.

WHAT'S ALREADY SOLID: --widescreen with deterministic view-cull instruction patches (see troublemakers.us1.toml [patches]) + RT64 wing-clear (patches/rt64/0001). The game's ATTRACT DEMO plays real level-1 gameplay unattended starting ~25-30s after boot — your window into in-level rendering, fully headless-compatible.

KNOWN LAYER-SYSTEM FACTS (verified): func_80082380 (static tile path) draws 7x10 grids of 32px tiles from a caller-passed array in $a3/ctx->r7 — BUT it serves multiple callers (func_80082CFC/80082E04/80082F10) with buffers that are NOT all backdrop grids (blind repacking corrupted sprites). func_80082820 (scrolling path) takes its buffer in $a0/ctx->r4 (call site func_80082F10:83C00 passes D_8017F310). N64Recomp supports [patches] instruction = [{func,vram,value}] AND hook = [{func,text}] (C injected at function entry; MEM_W(imm,reg) accessors; ctx->rN = args; ROM byte-verify every instruction you patch: .main vram V -> ROM offset V-0x80000400+0x1000).

YOUR WORKTREE HAS A PRIVATE RecompiledFuncs COPY — regenerate freely (tools/N64Recomp/build/N64Recomp troublemakers.us1.toml). Everything else symlinked READ-ONLY (privatize before modifying). Commit nothing. Build: cmake -B build -DMM_BUILD_GRAPHICS=OFF; run headless: MM_HEADLESS_GFX=1 SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy timeout -k 5 90 ./build/src/game/mm_game ./rom.z64 (cp ~/repos/trouble-makers-ai-recomp/baserom.us1.z64 ./rom.z64 first; 90s covers boot->attract-demo gameplay).

DELIVERABLE: PHASE8_NOTES_<lane>.md + RESULT block (gate: PASSED|PARTIAL|FAILED + two-line summary). The driver does all visual verification — deliver env-gated switches + exact driver instructions.

YOUR LANE (a — display-list forensics: from PIXELS back to code): Build the ground truth from what the game actually submits to the renderer. (1) Add an env-gated display-list dumper: in the headless stub renderer (src/game/main.cpp StubRendererContext::send_dl) you get the OSTask* — dump task->t.data_ptr/data_size and hexdump the DL bytes from rdram to a file per frame when MM_DUMP_DL is set (rdram pointer: capture it like register_overlays.cpp on_game_init does; aligned word reads are host-order). (2) Write a Fast3D display-list decoder (Python or C, your choice) using the decomp's ultralib/include/PR/gbi.h opcode encodings (this game = original F3D: G_TEXRECT 0xE4, G_SETTILE, G_SETTIMG, G_VTX/G_TRI1, G_DL 0xDE sub-lists — FOLLOW G_DL nesting!). (3) Decode a frame from the ATTRACT DEMO (~40-60s in) and produce the draw inventory: for every texrect/tri covering the mid-band screen region (y roughly 60-160 in 320x240 space), report screen x/y/w/h, source image address (G_SETTIMG), and tile params. The band's draws will show their source buffers => cross-reference which rdram arrays feed them and which layer path (static vs scrolling vs other) they belong to. Deliverable: the definitive "band = these draw commands from these buffers" map + dumps + decoder tool committed to your worktree (tools/dl_decode.py or similar) with usage docs.
